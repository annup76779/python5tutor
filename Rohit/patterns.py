"""

"""

n = int(input("Enter the number of rows: "))

for i in range(n): # 0 to n - 1
    row = ""
    for j in range(n):
        row += "**"  # end default value in \n -> new line [Enter]
    print(row)
